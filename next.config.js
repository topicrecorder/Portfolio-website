/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = {
  assetPrefix: './',
  basePath: '/Portfolio-website', // Replace 'your-repo-name' with your actual repository name
};
